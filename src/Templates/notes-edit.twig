{% extends "layout.twig" %}

{% block head %}

    <script src="https://cdnjs.cloudflare.com/ajax/libs/tinymce/5.7.1/tinymce.min.js" referrerpolicy="origin"></script>

{% endblock head %}

{% block title %}

    Edit note

{% endblock title %}

{% block main %}

    <main class="container" style="min-height: 75vh;">

        <h1>Edit note</h1>

        <form method="post" novalidate>
            <div class="row g-2">
                <div class="col-12 form-floating">
                    <input id="name" type="text" name="name" class="form-control bg-dark text-white" value="{{ note.name }}"/>
                    <label for="name">Name of note</label>
                </div>

                <div class="col-12 form-floating">
                    <label for="tiny"></label><textarea id="tiny" name="content">{{ note.content | raw }}</textarea>
                </div>
            </div>
            <div class="container">
                <input type="submit" class="btn btn-dark btn-lg btn-outline-primary m-3" value="Save"/>
            </div>
        </form>
    </main>

    <script>
        tinymce.init({
            selector: "textarea#tiny",
            skin: "oxide-dark",
            content_css: "dark",
            height: "600",
            statusbar: false,
            menubar: false,
            toolbar: 'undo redo | styleselect | forecolor backcolor | bold italic | alignleft aligncenter alignright alignjustify | outdent indent | link | code | table | fullscreen',
            plugins: 'link, code, table, fullscreen',
            setup: editor => editor.on('change', e => editor.save())
        });
    </script>

{% endblock main %}